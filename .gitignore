# Build output
dist/
.astro/

# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Vercel
.vercel/

# Session handoffs (per-session scratch, not checked in)
SESSION_HANDOVER.md
.playwright-mcp/
.claude/scheduled_tasks.lock

# Yaegi WASM artifacts: intentionally NOT ignored. They ship with
# the repo so Vercel deploys them without needing a Go toolchain in
# the build image. Regenerate locally via `pnpm runtime:build` after
# any change to runtime/yaegi-wasm/. ~11 MB raw / ~1.9 MB brotli on
# the wire. Migrate to a Vercel build hook (custom container with
# Go) when the size starts to bite or strict-fresh builds become
# valuable.

# Zig WASM artifacts: same model as yaegi — intentionally NOT
# ignored, so Vercel deploys them without needing a Zig toolchain.
# Regenerate locally via `pnpm runtime:zig:build` after any change
# to runtime/zig-wasm/. ~7 MB raw / ~3.4 MB brotli on the wire
# (zig.wasm + libcompiler_rt.a + zig-stdlib.tar.gz); the stdlib
# tarball is lazy-loaded by the worker on first eval.


# Playtest screenshots (gitignored — Playwright MCP outputs)
playtest-*.png
page-*.png
step*.png
.playwright-mcp/
