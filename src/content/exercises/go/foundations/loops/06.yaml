target: go
themeId: go/foundations/loops
type: fill-line
order: 6
prompt: |
  Type the Go line inside the loop that exits early when `i` reaches
  `stopAt`. The program processes 0..n-1 and prints each value via
  `process(i)`. With `n=5` and `stopAt=3` it should print `0`, `1`,
  `2` on separate lines.
generator:
  kind: template
  vars:
    line:
      - "if i == stopAt { break }"
  ts: |-
    for (let i = 0; i < n; i++) {
      if (i === stopAt) break;
      process(i);
    }
  canonical: |-
    package main

    import "fmt"

    func process(i int) {
    	fmt.Println(i)
    }

    func main() {
    	n := 5
    	stopAt := 3
    	for i := 0; i < n; i++ {
    		${line}
    		process(i)
    	}
    }
  distractors:
    - match: "if (i == stopAt) { break }"
      explain: |-
        Go doesn't take parens around the condition of an `if`. Drop the
        parens; the braces stay.
    - match: "if i === stopAt { break }"
      explain: |-
        Triple-equals is a JavaScript / TypeScript thing. Go uses `==`
        for equality; the types either match or it won't compile.
    - match: "if i == stopAt: break"
      explain: |-
        That's Python's shape — colon + indent for the body. Go uses
        braces: `if cond { body }`. No colon.
    - match: "break if i == stopAt"
      explain: |-
        Ruby's trailing-conditional reads nicely, but Go isn't Ruby.
        Lead with `if`, body in braces — even one-liners like `break`.
expectStdout: |
  0
  1
  2
runtime: yaegi
blanks: [line]
hints:
  - "Three things all need to be right at once: no parens around the condition (Theme 4 covered this), `==` not `===` for equality (also Theme 4), and braces required around the `break` body even though it's a single statement."
  - "The Go shape is `if condition { body }`. Bare keyword `break` stays the same as TS. Braces always."
  - "Type `if i == stopAt { break }`."
notes: |
  Migrated to input+Yaegi UX. Scaffold provides `process` that prints
  each value. Distractors kept as wrong-pattern bank.
